{
  "name": "steam-viewer-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Runs on your gaming PC so the Steam Viewer site can list and launch your installed games.",
  "type": "module",
  "bin": {
    "steam-viewer-agent": "src/index.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "//optionalDependencies": "ffmpeg-static provides the screen encoder. It is optional so a failed download never breaks the install; the agent fetches it on first run if it is missing.",
  "optionalDependencies": {
    "ffmpeg-static": "^5.2.0"
  }
}
